# CPU ERP

ระบบ ERP ของโรงงานเสื้อผ้ากีฬา — ผลิต / คลัง / ขาย / บัญชี ครบในตัวเดียว
เจ้าของคือ Boong ใช้งานจริงทุกวันกับพนักงานหลายคนพร้อมกัน **ทุกอย่างในนี้คือ production**

- **Stack** — React 19 (CRA) + Firebase (Firestore / Storage / Auth) + Vercel
- **Deploy** — `cpuerp.vercel.app` (โปรเจกต์ `cpu-erp`) · `boongerp.vercel.app` (โปรเจกต์ `boong-private`)
- **ขนาด** — 97 ไฟล์ · ~35,200 บรรทัด · `src/App.js` ~7,200 บรรทัด (ใหญ่สุด, handler ธุรกิจเกือบทั้งหมดอยู่ในนี้)

---

## กฎเหล็ก — ห้ามพลาด

1. **เขียนคอมเมนต์เป็นภาษาไทย** ให้ตรงกับที่มีอยู่ · อธิบาย **"ทำไม"** ไม่ใช่ "ทำอะไร"
2. **`CI=false npx react-scripts build` (หรือ `npm run build`) ให้ผ่านก่อน commit ทุกครั้ง**
3. **`git pull --rebase` ก่อน push เสมอ** — มี session อื่นแก้ repo เดียวกันอยู่ ชนกันบ่อย
4. **ห้ามแก้ยอดในเอกสารที่ออกไปแล้ว** (บิล/ใบวางบิล) — ลูกค้าถือกระดาษอยู่ ต้องออกเอกสารใหม่มาหักแทน
5. **สร้าง/ลบข้อมูลจริงในแอป ต้องถามก่อน** — ทดสอบผ่านเบราว์เซอร์ได้ แต่อย่าเขียนข้อมูล
6. **snapshot ไม่เปลี่ยนตามต้นทาง** — บิล/ใบสั่ง/รอบแพ็ค เก็บชื่อ+ราคา ณ ตอนบันทึกไว้ในตัวเอง เปลี่ยนชื่อรุ่นในคลังแล้วเอกสารเก่าต้องไม่ขยับ
7. **เพิ่ม collection ใหม่ → ต้องเติมใน `.github/workflows/firestore-backup.yml` ด้วย** ของที่ไม่อยู่ในลิสต์ = ไม่มีสำเนา หายแล้วหายเลย

---

## โมดูล (19 tabs ใน `src/tabs/`)

| กลุ่ม | หน้า |
|---|---|
| คลัง & ผลิต | ClothingInventory · Stocktake · Production (Kanban) · ProductionHistory · **PackRun** |
| ขาย | Orders (ใบสั่งของ) · Invoice (บิล) · Statement (วางบิล) · **Returns** (รับคืน) · CatalogInbox |
| อื่น | Customers · Transactions · Suppliers · Barcode · TaxDocs · Employee · Payroll · Reports · AuditLog |

**Catalog** (`src/Catalog.js`) เป็นหน้าสั่งของสาธารณะของลูกค้า แยกจาก ERP — ไม่ต้อง login, ไม่โชว์ราคา, ไม่โชว์สต๊อก

### Firestore collections
`products` `clothing` `transactions` `customers` `suppliers` `orders` `invoices` `statements`
`productionOrders` `customOrders` `boms` `returns` `packRuns` `packAliases` `catalogOrders`
`employees` `attendance` `payrollRuns` `taxDocs` `pendingMixSales` `users` `settings` `auditLog` `counters`

> `firestore.rules` อยู่ในรีโปแต่ **ไม่ได้ deploy อัตโนมัติ** — เพิ่ม collection ใหม่ต้องไป Publish ที่ Firebase Console เอง (ทำกับทั้ง 2 โปรเจกต์)

---

## เรื่องที่ต้องเข้าใจก่อนแก้

### 📦 รอบแพ็ค (PackRun) — ลูกค้าขายบน Shopee/TikTok/Lazada แล้วส่งงานมาให้แพ็ค
- **6-7 เจ้า × 200+ ออเดอร์/วัน** — ถ้าสร้างใบสั่งจริงจะ 1,200+ ใบ/วัน ระบบพัง
- จึงเก็บเป็น **ตัวนับ** `counts: { "clothingId|colorIdx|size": qty }` ไม่ใช่รายการ
  ใช้ `FieldPath` + `increment` เพื่อให้แพ็คพร้อมกันหลายเครื่องแล้วยอดไม่ทับกัน
- ทางเข้าหลัก = **ลากใบปะหน้า PDF** (`utils/pdfLabels.js` → `utils/packImport.js`)
- ปิดรอบ = ตัดสต๊อกครั้งเดียว + ออกบิลใบเดียว · เลือก "ปิดแต่ยังไม่ตัดสต๊อก" ได้

**การจับคู่ชื่อสินค้า** (`packImport.js`) — โจทย์ยากที่แก้ไปแล้ว อย่ารื้อ:
วรรณยุกต์หายจาก PDF · `ดํา`≠`ดำ` · `K11`↔`K-11` · `3L`↔`3XL` · สีติดชื่อ `โคตรถูก8ดำ` · แขนยาว/สั้นอยู่ในชื่อรุ่น
- ยึด **คลังเป็นตัวตัดสิน** ไม่ใช่รูปแบบข้อความ · ไม่มั่นใจต้องให้คนดู ห้ามเดาเงียบ ๆ
- คนแก้/ยืนยันแถวไหน → **จำไว้** ที่ `packAliases/{customerId}` + `packAliases/_shared`
  แก้ย้อนหลังได้ที่ `PackAliasesModal`
- **`pdf.worker.min.mjs` ต้องอยู่ใน `public/`** (ก็อปโดย `scripts/copy-pdf-worker.js` ตอน build)
  ห้ามให้ webpack จัดการ — CRA จะเอาไปผ่าน Babel แล้วเขียน import เป็น path ของเครื่อง build

### ↩️ รับคืนสินค้า (Returns)
เส้นเงินกับเส้นของ **แยกกัน**:
- เงิน: `status` รอจับคู่บิล → จับคู่แล้ว
- ของ: `qcStatus` รอตรวจ → ตรวจแล้ว (**เข้าสต๊อกตอนนี้** ไม่ใช่ตอนจับคู่บิล)

`settleMode` เลือกทางเดียว ห้ามซ้อน:
- `statement` → หักในใบวางบิลงวดถัดไป (ค่าเริ่มต้น)
- `cash` → จ่ายเงินคืน + ออกใบลดหนี้ · **ถูกกันออกจากใบวางบิลอัตโนมัติ**

**โควตาคืนของ** (`returnableMap` / `checkReturnAgainstInvoice` ใน `utils/returns.js`)
- คืนเกินที่ขายไปในบิล = ลดหนี้เกินจริง + สต๊อกเกิน → **ล็อกปุ่มบันทึก ไม่ใช่แค่เตือน**
  (เคสจริง: บิลมี 1 ตัว แต่คืนมา 13 ตัวได้)
- **ใบที่ยกเลิกแล้วต้องไม่กินโควตา** — ของถูกย้อนออกจากสต๊อกและไม่มีการลดหนี้แล้ว
  ถ้ายังนับอยู่ คนที่ยกเลิกใบทดสอบทิ้งจะคืนของชิ้นนั้นไม่ได้อีกเลย
- ชิปในบิลโชว์ **"เหลือ N"** (จำนวนที่คืนได้จริง) ไม่ใช่จำนวนที่ขายไป และหักที่กรอกค้างในฟอร์มด้วย
- admin ลบใบที่ **ยกเลิกแล้ว** ทิ้งถาวรได้ (ล้างใบทดสอบ) — แต่ลบไม่ได้ถ้าเคยหักในใบวางบิล /
  ออกใบลดหนี้ / จ่ายเงินคืนไปแล้ว

### 📥 การโหลดข้อมูล — อย่ารื้อ

**ปริมาณจริงของร้านคือ 2,500 ใบ/เดือน** (บิล + ใบสั่งของ ที่เขียนมือ) ตัวเลขในระบบยังน้อยกว่านี้มาก
เพราะยังคีย์เข้าไม่ครบ — ทุกการตัดสินใจเรื่องการโหลดยึดตัวเลข 2,500 ไม่ใช่ที่เห็นในระบบตอนนี้

**หลักการ: กองที่โหลดค้างไว้มีไว้ให้งานประจำวันเท่านั้น ที่เหลือไปขอเอง**

| ใคร | ได้ข้อมูลจากไหน |
|---|---|
| ออกบิล / รายการบิล | กองที่โหลดค้าง (7 วัน · กดขยายได้ที่ `LoadRangeBar`) |
| ใบวางบิลทั้งเดือน | `fetchInvoicesForPeriod()` — ดึงตามงวดตอนกด |
| รายงาน / VAT / Aging | `fetchInvoicesForPeriod()` — ตามช่วงที่เลือกเอง |
| โปรไฟล์ลูกค้า · จับคู่ใบคืนของ · ด่านกันบิลซ้ำ | `fetchInvoicesOfCustomer()` — ย้อนได้ไม่จำกัด |

⚠️ **กับดักวันที่ 2 ชุด** (`src/utils/fetchInvoices.js` อธิบายไว้เต็ม)
- `date` = วันที่บนหน้าบิล · **ย้อนหลังได้** · เป็นตัวที่ใบวางบิลใช้คิดยอด
- `createdAt` = วันที่กดบันทึก · **เป็นอันเดียวที่ Firestore ค้นเป็นช่วงได้**

ดึงด้วย `createdAt` ตรงขอบงวดเป๊ะ ๆ = บิลที่ลงวันที่ย้อนหลังหลุด → ลูกค้าโดนวางบิลขาด
จึง **ดึงเผื่อหัวท้าย 30 วัน แล้วให้ตัวกรองเดิมตัดด้วย `date` อีกชั้น** — Firestore ตัดของที่ไกลเกินเท่านั้น
(ทดสอบแล้ว: บิลลงวันที่ 31/08 ที่คีย์เข้าจริง 05/09 ต้องเผื่ออย่างน้อย 7 วันถึงไม่หลุด)

⚠️ **ห้ามลดหน้าต่างโหลดโดยไม่เช็คว่าใครพึ่งมันอยู่** — ตอนลด 30→7 วันมี 7 จุดที่ใช้รายการบิลนี้
ถ้าลดเฉย ๆ จะพังเงียบ ๆ: รายงาน VAT ผิด · จับคู่ใบคืนของกับบิลเก่าไม่ได้ · ประวัติลูกค้าหาย ·
**ด่านกันบิลซ้ำที่เขียนว่ากัน 30 วันจะเหลือ 7 วัน**

🖼️ **รูปห้ามเก็บลงเอกสาร** — ตอนอัป Storage ไม่สำเร็จ ระบบ fallback เป็น base64 ในเอกสาร (ตั้งใจ ไม่ให้รูปหาย)
แต่ทำให้เอกสารบวมจนเฉียดเพดาน 1 MB และถูกโหลดซ้ำทุกครั้ง
ล้างด้วย **⚙️ ตั้งค่า → 🖼️ ย้ายรูปฝัง** (`EmbeddedImageMigrator`) — สแกนก่อนเสมอ ต้องกดยืนยัน

### 🧾 บิล & ใบวางบิล
- **ยกเลิก ดีกว่า ลบ** — เลขที่ไม่ขาดช่วง
- สิทธิ์: manager ยกเลิกได้ · บิลชำระแล้วลบได้เฉพาะ admin · staff กด "ขอยกเลิก" แล้วรออนุมัติ (`cancelRequest` บนตัวบิล)
- ลบไม่ได้ถ้าบิลอยู่ในใบวางบิลแล้ว
- กันบิลซ้ำ 3 ชั้น: ปุ่มล็อกตอนบันทึก · เตือนถ้าลูกค้า+ยอดซ้ำใน 30 วัน · ป้าย 🔁 ในรายการ
- ใบวางบิลหักของคืนอัตโนมัติ แล้ว **ปั๊ม `appliedStatementId`** กันหักซ้ำ · ลบใบวางบิล = ปล่อยกลับ
- **บังคับผูก `customerId`** ก่อนออกบิล/ใบสั่ง/ใบ custom — ชื่อลอย ๆ ทำให้ร้านเดียวได้ใบวางบิล 2 ใบ
  และไม่มีที่อยู่ให้พิมพ์ · ซ่อมบิลเก่าที่ไม่ผูกได้ที่ `LinkInvoiceCustomers` (แก้ `customerId` เท่านั้น ไม่แตะชื่อที่พิมพ์ไว้)
- **`billingType` เงินสด/เครดิต** โชว์เป็นป้ายบนจอทุกที่ (`BillingBadge`) — **ห้ามใส่ในเอกสารที่พิมพ์**
  เป็นข้อมูลภายใน ลูกค้าไม่ต้องเห็นว่าเราจัดเขาเป็นประเภทไหน · ลูกค้าเงินสดถูกกันออกจากใบวางบิลอัตโนมัติ

**⚠️ ของที่แก้ยากไปแล้ว — อย่ารื้อ**

- **เจ้าของบิลมีได้รายเดียว** (`buildStatementGroups` ใน `utils/statement.js`)
  helper จับคู่ลูกค้าเป็นชั้น ๆ (รหัส → เบอร์ → ชื่อ) บิลใบเดียวจึงเข้าเงื่อนไขของหลายรายพร้อมกันได้
  ของเดิมสร้างแถวให้ทุกรายชื่อ → ยอดชุดเดียวกันโผล่ 2 แถว ติ๊กทั้งคู่ = **ทวงเงิน 2 รอบ (เคยเกิน ฿1.12M)**
  ตอนนี้เลือกเจ้าของไว้รายเดียว: ชั้นที่แน่นกว่าชนะ · เสมอกันให้ลูกค้าในทะเบียนชนะบิลลอย ๆ
- **FBT = 3 บริษัทคนละเจ้า** ชื่อคล้ายกันในทะเบียน **ห้ามรวม** (ดู memory `fbt-three-separate-companies`)
- **เลขเอกสารมาจาก `reserveDocNo` + `counters`** (transaction) ไม่ใช่ `Date.now()`
  ถ้า counter ใช้ไม่ได้จะ fallback เป็น "max+1 จากที่โหลดมา" ซึ่งในลูปจะได้เลขซ้ำ —
  ที่ออกหลายใบรวดเดียวต้องกันเอง (`nextUnused` ในใบวางบิล · seed สะสมตอนแยกบิลอุปกรณ์)
- **`bulkRunId` / `bulkRunAt`** ปั๊มบนใบวางบิลที่ออกพร้อมกัน — มีไว้ให้ "↩️ ถอยทั้งรอบ"
  กดพลาดทีเดียว 70-80 ใบ ถ้าต้องไล่ลบทีละใบคนจะยอมปล่อยผิดไว้แทน
  ถอยได้เฉพาะ **admin** · ใบที่ "เก็บเงินแล้ว" ถอยไม่ได้ · เลือกถอยเฉพาะบางใบก็ได้

### 🏐 แยกบิลอุปกรณ์กีฬา
ที่ร้านเปิดบิลอุปกรณ์กีฬาแยกกับบิลเสื้อผ้ามาตลอด ระบบจึงออกให้ 2 ใบเหมือนที่ทำมือ (`utils/billGroup.js`)

- ติ๊กรายรุ่นที่หน้าคลัง (`isEquipment`) · รุ่นที่ยังไม่เคยติ๊กเดาจาก `sizeType === "shoe"` ให้ก่อน
- **ใบเสื้อผ้าเป็นใบหลัก** — ส่วนลดท้ายบิล/ค่าส่ง/ค่าออกแบบ/มัดจำ อยู่ใบนั้นใบเดียว ไม่ให้คิดซ้ำ
  ยกเว้นส่วนลด **%** ที่คิดจากยอดของแต่ละใบเอง จึงใช้ได้ทั้งสองใบ
- ทั้งสองใบใช้เลขชุดเดียวกัน (ติดกัน) · ผูกกันด้วย `splitGroupId` / `splitSiblingNo`
- **`splitSiblingNo` ห้ามขึ้นบนกระดาษ** — ลูกค้าต้องเห็นเป็นบิลอิสระ 2 ใบ

### 🖨️ การพิมพ์ (`utils/print.js`)
- `tfoot` = `table-row-group` → ยอดรวมพิมพ์**หน้าสุดท้ายหน้าเดียว** (ยกเว้น `.bill-id-footer` ที่ต้องซ้ำทุกหน้า)
- สำเนาที่จะพิมพ์ถูกดึงกลับเข้าหน้ากระดาษด้วย `bringIntoPage()` — บางหน้าวาดเนื้อหาไว้นอกจอ
- `fontScale` ค่าเริ่มต้น 1.3 (สำหรับป้าย) — **ตารางเต็มหน้าให้ส่ง 1.0**

### 💾 กันข้อมูลหาย
- `overscroll-behavior-y: none` กัน pull-to-refresh บนแท็บเล็ต
- `useFormDraft` เก็บร่างใบสั่งของ/บิล/ใบสั่งผลิต custom ลงเครื่อง — ทิ้งเมื่อบันทึกสำเร็จหรือปิดเอง
- โหลดข้อมูลเป็น **ช่วงวันที่** ไม่ใช่ `limit(N)` — ที่ 200-300 ใบ/วัน limit จะซ่อนข้อมูลเงียบ ๆ
  (รายละเอียดเต็มอยู่ที่ **📥 การโหลดข้อมูล — อย่ารื้อ** ด้านบน)
- **สำรองอัตโนมัติทุกวัน 09:00 น.** — GitHub Actions ดูด Firestore ทั้ง 24 collection ของทั้ง 2 โปรเจกต์
  แล้ว commit ลง `backups/<project>/YYYY-MM-DD.json` เก็บย้อนหลัง 60 วัน
  กดรันเองได้ที่ Actions → Firestore Daily Backup → Run workflow
  ปุ่ม 💾 Backup ในแอปเป็นตัวสำรองของสำรอง ไว้ดึงเก็บเองก่อนทำอะไรเสี่ยง
- **ชนเพดานโหลดต้องเตือน ห้ามเงียบ** — `statements`/`returns` โหลดด้วยเพดานจำนวนล้วน ไม่มีช่วงวันที่
  ให้ขยาย ชนเมื่อไรใบเก่าหายจากหน้าจอ (ที่ 50-80 ใบวางบิล/เดือน เพดาน 500 เต็มใน 7-10 เดือน)

---

## สภาพแวดล้อม

- Windows · **ไม่มี python3** — ใช้ `node -e` หรือเขียนสคริปต์ไฟล์แทน
- ไฟล์ปน CRLF/LF → สคริปต์แก้ไฟล์ต้อง `split(/\r?\n/)`
- heredoc ใน bash กิน backslash — regex ที่มี `\s` `\u` ให้ประกอบด้วย `String.fromCharCode(92)` หรือใช้ Write tool
- `App.css` / `index.css` มี `.scroll-col > * { flex-shrink: 0 }` — กล่อง scroll ที่เป็น flex column ต้องใส่ class นี้ ไม่งั้นการ์ดถูกบีบแบน

---

## ค้างอยู่

**เร่ง** — พนักงานบัญชีหลักป่วยหนัก ระบบต้องรับงานแทนทั้งหมดในอนาคตอันใกล้
- [ ] **เปิดใบวางบิลรอบจริงรอบแรก** — ยังไม่เคยทำจริงสักครั้ง ควรลองออก 1 ใบ พิมพ์ แล้ว "↩️ ถอยทั้งรอบ" ก่อน
- [ ] หน้า **"งานบัญชีเดือนนี้"** — เช็กลิสต์ วางบิล/ภ.พ.30/เงินเดือน/ประกันสังคม พร้อมกำหนดวัน (เสนอไว้ ยังไม่ทำ)
- [ ] อัปเดตคลังให้ครบ (ชื่อรุ่น/สี/ไซส์) — ตอนนี้ยังไม่ 100% ทำให้ประเมินความแม่นของการจับคู่ไม่ได้
- [ ] ตั้ง `billingType` ให้ครบ — เหลือ 11 จาก 258 ราย ที่ยังไม่ระบุ เงินสด/เครดิต
- [ ] ยืนยันว่า `INV6908-0154` นันเอสพี ฿30,275 เป็นเงินสดจริง (ถูกกันออกจากใบวางบิลอยู่)
- [ ] ติ๊ก "อุปกรณ์กีฬา" ให้ถุงเท้าอีก 2 รุ่นที่อยู่ฝั่ง apparel (ระบบเดาไม่ถึง)

**ไม่เร่ง**
- [ ] boong-private ยังมีรูปฝังในเอกสาร 2 รูป (0.33 MB) — กด 🖼️ ย้ายรูปฝัง ในโปรเจกต์นั้นด้วย
- [ ] โฟลเดอร์ `backups/` โต 1.1 GB · `.git` 145 MB — ยังไม่ถึงขั้นวิกฤต แต่วันหนึ่งต้องลดวันที่เก็บ
      (ตอนนี้ 60 วัน) หรือย้ายไปเก็บที่อื่นแทน GitHub
- [ ] โมดูลเวลาเข้างาน (คุยไว้ว่าใช้เครื่องแตะบัตร RFID ~300-500 บาท)
- [ ] BOM เฟส 3 — ตัดวัตถุดิบอัตโนมัติตอนผลิต
- [ ] แยก handler ออกจาก `App.js` ไป `src/actions/` (งานใหญ่ เสี่ยงชน session อื่น · ตอนนี้ 7,200 บรรทัด)

---

## บันทึกงาน 6-7 ก.ย. 2569

**ปัญหาที่เจอ:** ปริมาณจริงของร้านคือ 2,500 ใบ/เดือน (เขียนมือ) แต่ระบบออกแบบไว้บนตัวเลขที่เห็น
ในระบบตอนนี้ซึ่งน้อยกว่ามาก — พอคีย์เข้าครบจะชนเพดานและช้าจนใช้ไม่ได้

| | ก่อน | หลัง |
|---|---|---|
| โหลดตอนเปิดแอป | 9.84 MB | **0.48 MB** (+ โค้ด 0.33 MB) |
| เพดานบิล | 3,000 ใบ | 10,000 ใบ |
| หน้าต่างบิลที่โหลดค้าง | 30 วัน | 7 วัน |
| backup ครอบคลุม | 10 กอง | **24 กอง** |
| backup ขนาด/วัน | 21.3 MB | ~10.3 MB |

**ตัวเลขที่ต้องจำ:** ทดสอบกับข้อมูลจริงพบว่าถ้าออกใบวางบิลงวด ส.ค. ในวันที่ 3 ก.ย. ด้วยวิธีเดิม
จะได้ ฿1,813,775 แต่ที่ถูกต้องคือ **฿1,911,855** — ขาดไป **฿98,080** จากบิลต้นเดือน ส.ค. 10 ใบ
ที่หลุดหน้าต่าง 30 วันไปแล้ว นี่คือเหตุผลที่หน้าวางบิลต้องดึงข้อมูลของงวดเอง

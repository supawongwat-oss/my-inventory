# เปิดสิทธิ์ให้ดึงรูปจาก Storage ไปใส่ PDF ได้ (ทำต่อเมื่อจำเป็น)

> ⚠️ **ยังไม่ต้องทำ ถ้ารูปขึ้นใน PDF ปกติแล้ว**
>
> เอกสารนี้ไว้ใช้ตอนกด PDF แล้วขึ้นเตือนว่า "โหลดรูปมาใส่ไฟล์ไม่ได้"
> และใน Console ขึ้นข้อความประมาณ `Failed to fetch` / `CORS`
> จากที่ทดสอบไว้ bucket ปัจจุบันดูเหมือนจะยอมให้ดึงอยู่แล้ว จึงอาจไม่ต้องตั้งอะไรเลย

## อาการที่เอกสารนี้แก้

กด PDF บิลที่มีรูปงาน custom แล้ว **ช้ามาก** และ **ไฟล์ที่ได้ไม่มีรูป**

## ทำไม

รูปงานเก็บอยู่บน Firebase Storage ซึ่งเป็นคนละโดเมนกับตัวเว็บ
ตัวสร้าง PDF ต้องดึงรูปมาวาดลงไฟล์ แต่เบราว์เซอร์จะบล็อกการดึงข้ามโดเมน
ถ้าฝั่ง Storage ไม่ได้ประกาศอนุญาตไว้ (เรียกว่า CORS)

- แสดงรูปในหน้าเว็บ → ไม่ต้องขออนุญาต จึงเห็นรูปปกติ
- ดึงรูปไปใส่ไฟล์ PDF → ต้องขออนุญาต ถ้าไม่มีก็ได้ไฟล์เปล่า

เป็นการตั้งค่าฝั่ง Firebase **ครั้งเดียวจบ** ไม่ใช่เรื่องที่แก้ในโค้ดได้

## วิธีทำ (ทำครั้งเดียว)

ต้องมี Google Cloud CLI ก่อน — โหลดจาก https://cloud.google.com/sdk/docs/install

```bash
gcloud auth login
```

```bash
gcloud storage buckets update gs://cpu-erp.firebasestorage.app --cors-file=cors.json
```

ถ้าใช้ `gsutil` รุ่นเก่า:

```bash
gsutil cors set cors.json gs://cpu-erp.firebasestorage.app
```

## ตรวจว่าสำเร็จไหม

```bash
gcloud storage buckets describe gs://cpu-erp.firebasestorage.app --format="default(cors_config)"
```

แล้วลองกด PDF บิลที่มีรูปอีกครั้ง — ควรได้รูปครบและเร็วขึ้นมาก

## หมายเหตุ

`cors.json` อนุญาตเฉพาะ 2 ที่ คือเว็บจริง (`cpuerp.vercel.app`) กับตอนพัฒนา (`localhost:3000`)
และอนุญาตแค่ **อ่าน (GET)** อย่างเดียว — อัปโหลด/ลบยังต้องผ่านการล็อกอินเหมือนเดิม
ถ้าเปลี่ยนโดเมนเว็บเมื่อไหร่ ต้องแก้ไฟล์นี้แล้วสั่งคำสั่งข้างบนใหม่
